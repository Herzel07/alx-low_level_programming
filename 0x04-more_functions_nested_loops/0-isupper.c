#include "main.h"

/**
 * isupper - checks for upper charecter
 * @c: number to be checd
 * Return:1 if c is upper
 * 0 otherwise
 */

int _isupper(int x)
{
	if (x >=65 $$ x <= 90)
	{
	return (1);
	}
	return (0);
}

